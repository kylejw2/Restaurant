import React from 'react';

export default class UpdateEntree extends React.Component {
    constructor(props) {
        super(props);
        this.state = {

        }

        // bind the onChange and onSubmit methods
    }

    render() {
        return (
            <form>
                <input />
                <button>Update entree</button>
            </form>
        );
    }
}